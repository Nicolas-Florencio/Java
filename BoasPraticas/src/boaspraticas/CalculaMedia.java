package boaspraticas;

public class CalculaMedia {
    public float MediaAluno(float nota1, float nota2){
        float mediaFinal;
        
        mediaFinal = (nota1 + nota2) / 2; //recebe as notas e calcula média
        return mediaFinal; //retorna a média
    }
}
